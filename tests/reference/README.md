# Reference Tests

Status. Structure only.

SPEC 01 tests will live here.

Fixture folders.

- `fixtures/xml/`
- `fixtures/html/`
- `fixtures/registry/`

Test groups to add during implementation.

- Parser unit tests against local XML and HTML fixtures.
- Resolver unit tests against a synthetic registry.
- One gated live GhaLII integration test.
