# Source

Future application code lives under `src/clauseguard/`.

Current package areas.

- `reference/` for SPEC 01 mirror and resolver.
- `parsing/` for the later bill parser and clause splitter.
- `retrieval/` for later corpus retrieval.
- `analysis/` for later risk classification and constitutional checks.
- `evidence/` for evidence card assembly.
- `api/` for the later FastAPI surface.
- `eval/` for labelling feedback and agreement checks.
