# Governance

This folder holds the living project guidance files.

- `CLAUDE.md` defines assistant rules and project constraints.
- `DECISIONS.md` records architecture decisions.
- `SKILLS.md` maps project capabilities to repo paths.

The PDFs in `docs/project/sources/` are source inputs. These Markdown files are the working versions used by the repo.
