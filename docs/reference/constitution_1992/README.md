# 1992 Constitution Reference

This folder will hold the fixed reference corpus.

Expected files.

- `raw.pdf`
- `raw.xml`
- `parsed.json`
- `articles/`

Each article file in `articles/` should contain the article text used for citation validation.

SPEC 01 requires the GhaLII mirror to prefer Akoma Ntoso XML. The existing PDF is a source document. The XML mirror becomes the canonical parsed source once implemented.
